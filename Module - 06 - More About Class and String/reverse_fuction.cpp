#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int n;
    // cin >> n;
    // int arr[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> arr[i];
    // }
    // reverse(arr, arr + n);
    // for (int i = 0; i < n; i++)
    // {n 
    //     cout << arr[i];
    // }
    
    string s; 
    cin >> s;
    reverse(s.begin(), s.end());
    cout << s;
    return 0;
}